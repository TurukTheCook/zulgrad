/**
 * --- IMPORT STANDARDS
 */
import mongoose from 'mongoose'
import jwt from 'jsonwebtoken'
import { uniq } from 'lodash'
import helper from './../helpers'

/**
 * --- IMPORT MODELS
 */
import User from '../models/user/user'
import NewsRequest from '../models/requests/newsRequest'

/**
 * --- EXPORT
 */
export default {
  /*
  * --- READ ALL
  */
  readAll(req, res) {
    // TODO
  },

  /**
   * --- READ ONE
   */
  readOne(req, res) {
    // TODO
  },

  /**
   * --- ADD
   */
  add(req, res) {
    // TODO
  },

  /**
   * --- DELETE
   */
  del(req, res) {
    // TODO
  }
}